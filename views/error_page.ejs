<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Error' %> - SwiftRoute</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"/>
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 sm:p-12 rounded-lg shadow-lg text-center max-w-md w-full">
        <svg class="w-16 h-16 text-red-500 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>

        <h1 class="text-2xl sm:text-3xl font-bold text-gray-800 mb-2">
            <%= typeof title !== 'undefined' ? title : 'An Error Occurred' %>
        </h1>
        <p class="text-gray-600 mb-6">
            <%= typeof message !== 'undefined' ? message : 'Sorry, something went wrong on our end.' %>
        </p>
        <a href="/" class="inline-block px-6 py-2 bg-blue-600 text-white font-semibold rounded-md hover:bg-blue-700 transition duration-150">
            Go Home
        </a>
         <% if (process.env.NODE_ENV !== 'production' && typeof error !== 'undefined' && error) { %>
             <pre class="mt-6 text-left text-xs text-gray-500 bg-gray-100 p-4 rounded overflow-auto"><code><%= JSON.stringify(error, null, 2) %></code></pre>
         <% } %>
    </div>
</body>
</html>